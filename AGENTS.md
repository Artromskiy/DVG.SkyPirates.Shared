# SkyPirates.Shared agent guide

This is game-specific shared code, not part of the reusable Delta stack. Do not
pull game types into Delta libraries or apply engine-wide migrations here
without an explicit task.

Use [WORKFLOW.md](WORKFLOW.md) for the bounded build. `game-developer` applies
to gameplay architecture; use project-specific skills only when the task calls
for them.
